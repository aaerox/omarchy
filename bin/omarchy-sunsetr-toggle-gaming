#!/usr/bin/env bash
# Toggle between default and gaming sunsetr presets

current_preset=$(sunsetr status | grep "Active preset:" | awk '{print $3}' | tr -d '[:space:][:cntrl:]')

if [ "$current_preset" = "gaming" ]; then
    sunsetr preset default
else
    sunsetr preset gaming
fi