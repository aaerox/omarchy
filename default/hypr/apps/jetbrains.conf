# JetBrains windows default size
windowrule = match:class (.*jetbrains.*)$, match:title ^$, size 50% 50%

# Fix tab dragging (tab titles are just one space)
windowrule = match:class ^(.*jetbrains.*)$, match:title ^\\s$, no_initial_focus true
